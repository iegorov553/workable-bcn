# Проверка кофеен — 11 сентября 2026

В исходной карте было 432 места. После сверки — **520**: 88 отсутствовавших точек добавлено, **351** есть в официальных каталогах, **169** пока не подтверждены. «Добавлено» означает отсутствовало в исходной базе, а не обязательно открылось недавно. «Есть в каталоге» не означает «открыто сейчас».

| Сеть | Всего | В официальном каталоге | Не подтверждено |
|---|---:|---:|---:|
| 365 Café | 173 | 156 | 17 |
| Buenas Migas | 18 | 18 | 0 |
| El Fornet | 55 | 41 | 14 |
| Granier | 63 | 58 | 5 |
| Sandwichez | 23 | 21 | 2 |
| Santagloria | 65 | 57 | 8 |
| Vivari | 123 | 0 | 123 |

## Метод и источники

- [365 Obrador](https://365obrador.com/es/encuentranos/): официальный locator, его публичные результаты с разрешёнными фильтрами 100 результатов / 25 км из центра, северной и западной части агломерации. Повторяющиеся карточки с разными ID объединены. Совпадения улиц, номеров и координат; 13 угловых входов сопоставлены по совпадающей позиции в пределах 25 м. Это географическая сверка, не независимое подтверждение посещением.
- [Buenas Migas](https://www.buenasmigas.com/): все 18 страниц заведений из меню Focacceries. **Gignàs, 25 ошибочно относился к Sandwichez** в исходной карте. Исправлена сеть, старый ID оставлен для избранного.
- [El Fornet](https://elfornet.com/tiendas): 41 магазин и страницы спорных / отсутствовавших адресов. Общий список содержит неточности: например, карточка Llacuna 124 на странице заведения указывает **Llacuna 140–142**, а Pau Claris уточняется как **190**. Страницы заведений имеют приоритет. Для новых точек координаты взяты из опубликованных сетью ссылок на карты. Добавлены Sarrià 1–3, Pallars 193, Rambla 80 (Sabadell), Francesc Moragas 21 (Sant Cugat), Santa Esperança 11 (Granollers).
- [Granier](https://pansgranier.com/donde-estamos/): публичный `shopsArray.json`, используемый официальной картой. 58 точек в исследуемой области. Поле Barcelona в этом источнике означает провинцию, не обязательно город.
- [Sandwichez](https://www.sandwichez.com/locales): 21 адрес; офис отдельно не импортировался. Добавлены Sants 358 и Gràcia 1 (Sabadell). Capità Arenas 3 и Travessera de les Corts 305 отсутствуют в текущем списке. У Capità Arenas есть [вторичный сигнал возможного закрытия](https://es.restaurantguru.com/SandwiChez-Barcelona-6), но его недостаточно для достоверной пометки «закрыто навсегда». Обе записи оставлены неподтверждёнными.
- [Santagloria](https://www.santagloria.com/nuestros-locales): данные маркеров из официального сайта, 57 точек в области сверки. Точки внутри торговых центров с разными адресами/этажами сохранены отдельно. В нескольких карточках координаты расходятся с исходной картой; они отмечены в отчёте.
- [Vivari](https://vivari.es/): HTTPS-сертификат просрочен (`CERT_HAS_EXPIRED`), обход проверки TLS не использовался. Индексируемые официальные страницы рассказывают о сети, но не дают поадресного каталога, позволяющего проверить 123 точки. Их актуальность **не установлена**. Нельзя утверждать, что все они работают или что список новых Vivari полный.

Для координатных каталогов использована область 41.30–41.60 N, 1.90–2.30 E: Барселона и окрестности, а не вся Каталония. Сохранены исходные точки вне прямоугольника; полный официальный список El Fornet включает и Granollers. Поиск новых точек вне этой области по большим сетям не исчерпывающий.

## Сохранённые материалы

- `data/places-audit.csv`: строка для **каждого** итогового места — статус, дата, источник.
- `data/original-places.json`: неизменённые исходные 432 записи.
- `data/official-catalogs.json`: нормализованные официальные записи, ссылки и дата снимка.
- `data/catalog-comparison.json`: сопоставление исходных записей, расхождения координат, отсутствовавшие точки.
- `data/update-summary.json`: изменения адресов, сети и итоговые счётчики.

У статуса `listed` подтверждено наличие адреса на официальном сайте на дату снимка. У `unverified` нет достаточного подтверждения: отсутствие на сайте не доказывает закрытие. Эти точки остаются доступны с явной пометкой. Наличие Wi-Fi, свободных мест и разрешения работать с ноутбуком по каждому заведению не проверено; приложение больше не обещает это автоматически.

При расхождении координат более 150 м сохранена прежняя точка и добавлена заметка; такие маршруты стоит проверить отдельно. По адресам из официального каталога можно найти заведение даже при неточном маркере. Ни часы работы, ни коммерческий статус не выдумываются.

Повторная загрузка My Maps теперь формирует `data/import-candidates.json` и не перезаписывает проверенную базу. Скрипты `fetch-*`, `parse-official-catalogs.mjs`, `refine-catalog-details.mjs`, `compare-catalogs.mjs` документируют эту конкретную сверку; их HTML-парсеры и ручные уточнения необходимо пересмотреть при будущей смене сайтов. Они не являются автоматическим мониторингом закрытий.

## Верификация и навигация через Google Places API

Для подтверждения актуальности заведений и повышения точности пешей навигации реализован пайплайн сверки каталога через Google Places API (New) и маршрутизации к объектам POI.

### Принцип работы аудита (`npm run audit:places`)

Скрипт `scripts/audit-google-places.mjs` выполняет автоматизированную сверку всех заведений из `src/data/places.json`:
1. **Формирование поискового запроса**: для каждой точки очищается адрес (отсекается суффикс района через `·`) и составляется запрос вида `<сеть|название> <адрес> Barcelona`.
2. **Запрос к Google Places API (New)**: выполняется вызов `places:searchText` с маской полей (`id`, `displayName`, `formattedAddress`, `location`, `businessStatus`, `googleMapsUri`, `rating`, `userRatingCount`) и круговым гео-ограничением (`locationBias`) радиусом 150 метров вокруг исходных координат.
3. **Кэширование**: результаты запросов сохраняются в `.cache/google-places-cache.json`, предотвращая повторные платные запросы к API.
4. **Сопоставление координат (Haversine)**: скрипт находит ближайшего кандидата и вычисляет расстояние по формуле гаверсинусов:
   - Если ближайший кандидат находится в пределах 150 м, он считается совпадением.
   - Если кандидат дальше 150 м, регистрируется `DISTANCE_MISMATCH` для ручной проверки.
   - Если результатов нет, фиксируется `NOT_FOUND`.

### Выявление закрытий заведений

Google Places API возвращает поле `businessStatus`, позволяющее отслеживать актуальный статус точек:
- `OPERATIONAL` — заведение работает в штатном режиме.
- `CLOSED_TEMPORARILY` — заведение временно приостановило работу.
- `CLOSED_PERMANENTLY` — заведение закрыто навсегда.

Все обнаруженные закрытия и расхождения заносятся в структурированный отчёт `docs/data/google-places-audit.json`. Скрипт не удаляет закрытые точки автоматически во избежание ложных срабатываний: разработчику выводятся предупреждения с требованием явной ручной валидации перед удалением из каталога.

### Обогащение базы (`npm run audit:places:apply`)

При запуске с флагом `--apply` для подтверждённых действующих заведений (`OPERATIONAL`) в `src/data/places.json` добавляются поля:
- `googlePlaceId`: уникальный идентификатор места в Google Maps (например, `ChIJ...`). Корректность формата проверяется регулярным выражением в `scripts/check-places.mjs`.
- `googleMapsUrl`: каноническая ссылка на карточку заведения в Google Maps (`googleMapsUri`).

### Навигация к POI в мобильном приложении

При нажатии на кнопку «Маршрут» («Directions») в приложении утилита `src/utils/directions.ts` генерирует Google Maps Universal Directions URL:
- **При наличии `googlePlaceId`**: формируется URL с параметрами `destination=<Name, Address>&destination_place_id=<PlaceId>&travelmode=walking`. В этом случае Google Maps открывает не безымянную булавку на координатах, а официальную карточку заведения (POI) с актуальным графиком работы, загруженностью и отзывами.
- **При отсутствии `googlePlaceId`**: срабатывает обратная совместимость с переходом по координатам (`destination=<lat>,<lon>&travelmode=walking`).

## Масштабирование на агломерацию (AMB Cafe Discovery)

Для расширения охвата карты за пределы центра Барселоны на всю столичную область (Área Metropolitana de Barcelona, AMB) разработан автоматизированный пайплайн поиска, нормализации и дедупликации кофеен целевых сетей через Google Places API (New).

### Географический охват и границы

Поиск охватывает все **36 муниципалитетов AMB**, а также **10 административных районов Барселоны**:
- **Районы Барселоны (10)**: Ciutat Vella, Eixample, Sants-Montjuïc, Les Corts, Sarrià-Sant Gervasi, Gràcia, Horta-Guinardó, Nou Barris, Sant Andreu, Sant Martí.
- **Муниципалитеты AMB (35 городов-спутников + Barcelona)**: Badalona, Badia del Vallès, Barberà del Vallès, Begues, Castellbisbal, Castelldefels, Cervelló, Cerdanyola del Vallès, Corbera de Llobregat, Cornellà de Llobregat, El Papiol, El Prat de Llobregat, Esplugues de Llobregat, Gavà, L'Hospitalet de Llobregat, La Palma de Cervelló, Molins de Rei, Montcada i Reixac, Montgat, Pallejà, Ripollet, Sant Adrià de Besòs, Sant Andreu de la Barca, Sant Boi de Llobregat, Sant Climent de Llobregat, Sant Cugat del Vallès, Sant Feliu de Llobregat, Sant Joan Despí, Sant Just Desvern, Sant Vicenç dels Horts, Santa Coloma de Cervelló, Santa Coloma de Gramenet, Tiana, Torrelles de Llobregat, Viladecans.

Все найденные кандидаты фильтруются по гео-рамке AMB (bounding box):
`41.20 <= latitude <= 41.55` и `1.90 <= longitude <= 2.35`. Точки вне этих пределов отсекаются.

### Поисковая матрица и целевые сети

Пайплайн проверяет 7 ключевых сетевых брендов:
1. **365 Café** (поисковые термины: `365 Obrador`, `365 Cafe`)
2. **Granier** (`Granier`)
3. **Vivari** (`Vivari`)
4. **Santagloria** (`Santagloria`)
5. **El Fornet** (`El Fornet`)
6. **SandwiChez** (`Sandwichez`)
7. **Buenas Migas** (`Buenas Migas`)

Для каждого муниципалитета и района формируется точный поисковый запрос (всего 368 запросов). Названия найденных мест сверяются с регулярными выражениями брендов (`BRAND_PATTERNS`), исключая ложные совпадения.

### Кэширование запросов

Все ответы API кэшируются в `.cache/amb-discovery-cache.json` по ключу `${chain}:${municipality}:${searchTerm}` и тексту запроса:
- Первый запуск наполняет кэш, минимизируя расходы квоты Google Places API.
- Повторные запуски выполняются локально за доли секунды без сетевых запросов.
- Файл кэша исключён из системы контроля версий (`.gitignore`).

### Дедупликация и сопоставление (`deduplicateAgainstCatalog`)

Для каждого обнаруженного кандидата выполняется трёхступенчатая дедупликация против текущей базы `src/data/places.json`:
1. **Совпадение по Google Place ID**: если `id` кандидата уже ассоциирован с каким-либо заведением в каталоге, точка помечается как `alreadyInCatalog` и не дублируется.
2. **Гео-сопоставление по цепочке и расстоянию (<= 50 м)**:
   - Если в радиусе 50 м по формуле гаверсинусов (Haversine) уже есть заведение той же сети:
     - При наличии у существующего заведения `googlePlaceId` — точка признаётся известной (`alreadyInCatalog`).
     - При отсутствии у существующего заведения `googlePlaceId` — точка регистрируется как `enrichedExisting`. Это позволяет дообогатить существующие записи идентификаторами Google Place ID и ссылками без добавления дубликатов.
3. **Новый кандидат (> 50 м)**: заведение признаётся новой точкой (`newCandidates`). Для него:
   - Нормализуется адрес (`formatAmbAddress`): отсекаются суффиксы `Spain`/`España`, пятизначные почтовые индексы, дублирующиеся названия городов; добавляется суффикс `· <Municipality>`.
   - Генерируется уникальный детерминированный слаг: `${chainSlug}-${lat.toFixed(6)}-${lon.toFixed(6)}`.
   - Формируется структура с блоком `verification`: `{ status: "listed", checkedAt: "<date>", source: "google-places-discovery" }`.

### Команды CLI и безопасность обновления

Пайплайн управляется скриптом `scripts/discover-amb-cafes.mjs` и npm-командами:
- `npm run discover:amb`: режим сухого прогона (dry-run). Выполняет поиск по матрице, сверку с кэшем/API, дедупликацию и сохраняет полный структурированный отчёт в `docs/data/amb-candidates.json` без модификации рабочей базы.
- `npm run discover:amb:apply`: режим применения. Вносит обогащения в существующие записи и добавляет новых кандидатов в `src/data/places.json`. После записи автоматически запускается валидатор `scripts/check-places.mjs`. При обнаружении любых нарушений целостности базы выполняется **автоматический откат** к исходному файлу каталога.
- Флаги фильтрации: `--cities=...`, `--chains=...`, `--no-cache`, `--api-key=...`.

### Итоги первого прогона поиска (Сентябрь 2026)

Результаты полного сканирования матрицы AMB с фильтрацией ложных срабатываний и точным определением муниципалитетов:
- **Выполнено запросов**: 368
- **Найдено сырых кандидатов**: 4 315
- **Уже присутствуют в каталоге**: 447
- **Обогащаемые существующие точки**: 1 (`365-caf--41.369755-2.107968` в L'Hospitalet de Llobregat, совпадение 12 м)
- **Новых уникальных кандидатов**: 44

#### Распределение новых кандидатов по сетям

| Сеть | Новые кандидаты | Обогащение существующих |
|---|---:|---:|
| 365 Café | 19 | 1 |
| Santagloria | 10 | 0 |
| Granier | 8 | 0 |
| Vivari | 4 | 0 |
| El Fornet | 2 | 0 |
| Buenas Migas | 1 | 0 |
| SandwiChez | 0 | 0 |
| **Итого** | **44** | **1** |

> **Примечание по сетям:** благодаря строгому сопоставлению (`matchesBrand`) полностью исключены ложные кандидаты под маркой SandwiChez (строительные сэндвич-панели, кебабы, бургерные) и несетевые локальные пекарни под маркой El Fornet (типа «El fornet de la Lluïsa»).

#### Распределение новых кандидатов по муниципалитетам и районам

| Муниципалитет / Район | Новые кандидаты | Обогащение |
|---|---:|---:|
| L'Hospitalet de Llobregat | 8 | 1 |
| Castelldefels | 5 | 0 |
| Badalona | 4 | 0 |
| El Prat de Llobregat | 3 | 0 |
| Viladecans | 3 | 0 |
| Sants-Montjuïc (Barcelona) | 3 | 0 |
| Sarrià-Sant Gervasi (Barcelona) | 3 | 0 |
| Cornellà de Llobregat | 2 | 0 |
| Sant Boi de Llobregat | 2 | 0 |
| Sant Martí (Barcelona) | 2 | 0 |
| Esplugues de Llobregat | 2 | 0 |
| Gavà | 1 | 0 |
| Sant Feliu de Llobregat | 1 | 0 |
| Cerdanyola del Vallès | 1 | 0 |
| Montgat | 1 | 0 |
| Sant Vicenç dels Horts | 1 | 0 |
| Gràcia (Barcelona) | 1 | 0 |
| Eixample (Barcelona) | 1 | 0 |
| **Итого** | **44** | **1** |

### Сохранённые артефакты

- `docs/data/amb-candidates.json`: подробный JSON-отчёт с метаданными генерации, агрегированной статистикой, разбивками по сетям и городам, а также полным списком 44 кандидатов и 1 обогащения.
- `.cache/amb-discovery-cache.json`: персистентный кэш всех 368 запросов Google Places API для воспроизводимости и локального тестирования без затрат API-квоты.

